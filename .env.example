# ──────────────────────────────────────────────
# HexaCity — Environment Variables
# Copy to .env.local and fill in values as needed
# ──────────────────────────────────────────────

# ── Server-only (not exposed to browser) ─────
# Base URL of the deployed app (used for metadata, OG images, etc.)
SITE_URL=

# ── Client-safe (exposed to browser) ─────────
# Public base path for assets if served from a CDN
NEXT_PUBLIC_ASSET_PREFIX=
